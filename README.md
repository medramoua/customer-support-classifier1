This project is a text classifier that categorizes short customer support messages into Billing, Technical, or Account. It uses TF-IDF to convert text into numbers and Logistic Regression for classification.
The system also shows the top keywords influencing each prediction.

